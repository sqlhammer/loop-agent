services:
  eventmanager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - db-data:/data
    environment:
      - ConnectionStrings__Default=Data Source=/data/eventmanager.db

volumes:
  db-data:
